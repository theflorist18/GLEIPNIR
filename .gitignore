# dependencies / builds
node_modules/
dist/
*.tsbuildinfo
# chaincode build artifact (F1: a 20.6 MB ELF was accidentally committed once; never again)
chaincode/evidence/evidence

# generated crypto material and channel artifacts (regenerated by orchestration)
network/organizations/
network/channel-artifacts/
**/generated.env

# benchmark & run outputs (referenced by runId, not committed)
benchmark/results/
# generated per-case network configs only — case-template.yaml is SOURCE and stays tracked (F67)
benchmark/networks/case-[0-9]*.yaml

# thesis drafts (binary; kept out of the never-rewritten history)
Pre-Thesis Paper.*

# python
__pycache__/
*.pyc
.venv/

# misc
build-workflow.js
*.log
.DS_Store
.env.local
