# GLEIPNIR — Session Handoff (2026-09-26)

> **Date tag:** 2026-09-26 · **For:** the next working session (either author, or the next Claude session).
> **Read first:** this file, then `docs/handoff-2026-09-25.md` (the M27 app and design work this builds on) and
> `docs/CONTRACTS.md` §12-20 (today's contract changes). `CLAUDE.md` is the rulebook but it is **local and gitignored**;
> see §6 for the lines in it that are stale.

## 1. Where the repo stands

- **`main`:** both M27 commits from last session landed there and on `origin/main`:
  - `63d534b` (desktop app, dashboard removed)
  - `ba48923` (web redesign)
- **Today's code work** is `f441636` (branch `ponytail-refactor`). It is **merged into `main`** as `6073abc`, after
  this handoff (`9b8471f`).
- **`main` is pushed to `origin/main`** (GitHub), 2026-09-26.
- **The working tree is on `main`,** the same code the live stack is running (the refactored images).
- **Never rewrite history.** The thesis cites SHAs (CLAUDE.md, Repository discipline).

## 2. What this session did

### 2.1 Over-engineering audit and refactor (`f441636`, net −2,795 lines, −1 dependency)
- **Audit.** 12 agents audited the whole repo in 6 areas; each area had one agent proposing cuts and one checking them.
  - 109 candidate cuts; 105 survived verification; **104 applied**.
- **Skipped on purpose:** replacing `collect.py`'s `_percentile` with `statistics.quantiles`.
  - The stdlib version isn't bit-identical (p95 `20.049999999999997` became `20.05`, and some p50 values turn
    from int to float).
  - It would have changed the manifests of runs already collected.
- **Author sign-off** (recorded in CONTRACTS §12-20):
  - Apply the cuts that touch contracts, and update CONTRACTS/ARCHITECTURE to match.
  - Include the chaincode and workload cuts.
  - Cut the one-time database migrations.
  - Fix the verify-UI bug instead of freezing it.
- **What went:**
  - **Generated files no longer committed.**
    - The committed `benchmark/networks/parallel-c*.yaml` and `benchmark/benchmarks/smoke-*.yaml` are removed.
    - `experiment.network_config_path` now always renders the parallel config into the run folder.
    - `rounds.py` has no command line any more (`--static` removed).
  - **Orchestration scripts.**
    - `teardown-channel.sh`, `case-template.yaml`, `BATCH_N`/`BATCH_K`, `--fresh-network` and `collect.py`'s
      collect-time provenance fallbacks are removed.
    - `lib.sh` now holds `app_channel` / `deploy_app_chaincode`, used by `up.sh` and `provision-channel.sh`.
    - `peer_env` is one `case` statement.
  - **Compose files.** The orderer/peer/CA blocks are de-duplicated with `x-` anchors. Environment settings that
    did nothing are removed (logging, bootstrap, profile, metrics provider, `FABRIC_CA_HOME`, cli `tty`).
  - **Services.**
    - The case-registry M18/M19/M25/M25b startup migrations and backfills are removed.
    - Routes removed: evidence-store `/blobs/:id/meta` and `/verify`; case-registry
      `GET /cases/:id/categories` and `GET /evidence-index/:id`; `?limit`.
  - **Gateway.**
    - Removed: the optional-library modes, the M17 users migration and `SESSION_IDLE_TTL_SECONDS` (fixed at
      30 min now).
    - The batcher and receipts clients were merged into `serviceClients.js`.
    - Upload filenames are now decoded by multer's `defParamCharset: 'utf8'`.
  - **Frontend.**
    - Removed: 78 unused CSS tokens, dead API methods and types, `settings.tsx`, and the create-case and
      participants cards on Case admin (they duplicated My cases and the case Team section).
    - Added `TeamRoster.tsx` (shared by case detail and the lead dashboard) and `lib/save.ts`.
  - **Chaincode and workload.** `getRequired`/`putJSON` helpers and `fmt`/`timestamppb` in `keys.go`; one
    `opRequest` in the workload; `util.parseArgs`.
    - Behaviour is preserved: trace hashes and chaincode error strings are unchanged.
  - **Bench app.** Dead theme and parse code removed; the helpers are now reused from `experiment.py`.
- **Bug fixed.** The evidence page's Merkle verify controls could never be reached after M27, because the
  frontend's variant setting was never set.
  - `anchoring` now comes from the `batched` flag the gateway returns on each write.
  - Proven live (§2.3).
- **Cloud review of the branch (ultrareview):** 3 findings, none needed a code change.
  - Two files "missing": they were untracked at the time and are now committed.
  - The multer option being "unsupported": disproved by a real UTF-8 upload.
  - The trace-cache removal: regeneration costs about 0.1 s per run.

### 2.2 Full test (all green)
- **Offline tests:**

  | Suite | Result |
  |---|---|
  | gateway | 77/77 |
  | services | anchor-client 6, case-registry 18, evidence-store 7, merkle-batcher 10, receipt-store 8, verification 15 |
  | benchmark | 14/14 |
  | frontend | vitest 34/34; typecheck and build clean |
  | `test_experiment.py` / `test_benchapp.py` | pass on Windows and WSL |
  | `collect.py --selftest` | OK |
  | shell scripts | `bash -n` on all 11 |
  | chaincode (Go 1.25.5) | `go vet`, `go test`, `gofmt` clean |

  The lower counts are tests deleted along with the code they covered.
- **Equivalence checks:**
  - All 5 stored traces regenerate byte-identical.
  - The merged compose config differs only by the removed settings that did nothing.
  - `collect.py` re-run on copies of the E0 runs produces identical manifests.
  - The E0 CSV is identical.

### 2.3 Live run on the refactored images
- **Order:** commit → fresh backup → rebuild all 9 images → run E0 on all 4 variants → restore the backup.
- **Test data safety.**
  - The backup `backups/20260926-202333` was taken before any new image touched the volumes.
  - Before starting the new images, the backup was checked for every repair the refactor removed. It needed
    none: no `displayName` fields, the M18 `'lead'` role check is present, all M19 columns exist, no case
    without categories, no case without audit rows.
  - A fingerprint of the test data (users, cases, rosters, categories, and every fixture exhibit's audit trail,
    notes and index row) was taken four times: old code, new code, after the browser tour, and after the
    restore. **All four are identical:** 13 users, 8 cases, 24 exhibits, 29 trail events.
- **E0 was re-smoked on `f441636` for all four variants.** This counts as the re-smoke that §12-20 requires.
  - `benchmark/results/e0/` holds the new runs; the old ones are archived in
    `benchmark/results/e0-pre-ponytail-20260926/`.
  - Success/failure per round, new vs old:

    | Variant | New | Old | Wall time |
    |---|---|---|---|
    | standard | 20/0 · 248/0 · 100/0 · 240/0 | 20/0 · 248/0 · 96/0 · 239/1 | 455 s (was 396 s) |
    | anchoring | 20/0 · 248/0 · 100/0 · 240/0 | same | 351 s (was 271 s) |
    | parallel (c10) | 20/0 · 248/0 · 239/1 (known trace race) | same | 461 s (was 480 s) |
    | parallel-anchored (c10) | 20/0 · 248/0 · 100/0 · 240/0 | same | 452 s (was 456 s) |

  - The audit okRate is 1 on all four.
  - Parallel-anchored wrote to the anchor channel only; the app channels grew by 0 bytes.
  - `verify-metrics` has 100 lines per anchored run.
- **Web app:** checked in Chrome with injected session tokens, never by typing a password.
  - The new design is live in light and dark.
  - Pages checked: login, session-ended banner, My cases, case detail (Team roster, categories), Case admin,
    Users, lead dashboard, search, 4-step ingest (upload and "integrity verified end-to-end"), the evidence page
    (rails, preview, flag, notes, custody forms), the CoC report page, and the not-found / not-authorized cards.
  - No console errors.
  - No fixture exhibit was opened; the read-only tour called none of the four auto-logging routes.
- **Verify fix, live on anchoring.** "Verify latest" is disabled before a write and enabled after a batched one.
  - While the batch is open it shows "not yet anchored".
  - After `POST :4001/flush` it shows **VERIFIED** with the fetch / recompute / compare-root timings.
  - The API returns `200 {ok:true}` for the same event.
  - On standard the button stays disabled and the badge says N/A.
- **B1 (UTF-8 filenames), live:** `证拠-テスト-café.txt` round-trips exactly, and downloads carry an RFC 5987
  `filename*=UTF-8''…` header.
- **Smoke scripts:** `smoke-standard.sh` PASS; `smoke-library.sh` PASS on all 17 steps. Both ran inside the
  disposable window, so the restore removed everything they created.
- **Bench app:** screenshots of all 7 tabs show the new design. The ledger chip moved from "test data" to
  "benchmark:e0@…" and back, and History lists the new runs. The Preview (dry run) path planned 4 runs.

### 2.4 One failure along the way: stale proxy certificate bundle (environment, not code)
- **Symptom:** the first parallel-anchored run's verify round went 0/100 because the anchor-client container
  crashed on start (`Cannot find package @grpc/grpc-js`).
- **Cause:** `C:\Users\LENOVO\gleipnir-ca-bundle.crt` (generated 2026-07-04) no longer matches the host's HTTPS
  proxy.
  - Inside the image build, npm failed TLS (`UNABLE_TO_VERIFY_LEAF_SIGNATURE`) and died with
    "Exit handler never called!", **but exited 0**. The image shipped with an empty `node_modules`.
  - The other 8 images escaped only because their `npm ci` layers came from Docker's cache.
- **Fix used:**
  1. Regenerated a bundle from the Windows **Root + intermediate (CA)** stores (110 certificates) into the
     session's temp folder.
  2. Rebuilt anchor-client alone with `--no-cache`.
  3. Checked every dependency resolves inside the image.
  4. Re-ran E0 parallel-anchored: PASS.
- **The saved bundle file is still the stale one** (see §5).

## 3. Live environment right now

- **Stack:** up on **standard** (14 containers) and running the **refactored** images from `f441636`. Gateway at
  `:3000`, web at `http://localhost:8081`.
  - All gateway sessions from before the restore are gone, so sign in again.
- **Ledger:** holds the authors' **manual test data**, restored from `backups/20260926-202333` and verified
  identical. `backups/state.json` origin = "test data".
  - `backups/20260926-202333` is the only backup kept; the older ones were deleted.
- **Images:** only the refactored `gleipnir-*:latest` images remain. The `:pre-ponytail` rollback tags were
  deleted after the merge.
- **Baselines in `benchmark/sweeps.yaml` are still PLACEHOLDERS:** send rate 50, batch 50, channels 20,
  channels_max 50. `events_per_case_per_round` is 200.
- **`network/compose/.env` matches HEAD.** E0 runs and restores rewrite it, and it is back to the committed
  content.

## 4. How to run things

| What | How |
|---|---|
| Web app | `http://localhost:8081` (the stack must be up) |
| Bench app | double-click `orchestration/benchapp.pyw`, or `pyw -3.11 orchestration\benchapp.pyw` |
| Gateway tests | `cd gateway && npm test` (77/77) |
| Service tests | `node --test` inside each `services/<name>` |
| Frontend tests / build | `cd frontend && npx tsc --noEmit && npx vitest run && npx vite build` |
| Bench tests | `python orchestration/test_benchapp.py && python orchestration/test_experiment.py` (Windows or WSL) |
| Chaincode tests | Go 1.25.5 is **not installed** on the host or in WSL. Download the pinned zip to a temp folder, then run `go test ./...` in `chaincode/evidence` with `GOTOOLCHAIN=local` |
| Rebuild images | `GLEIPNIR_CA_BUNDLE=<fresh bundle> bash docs/audit/chunk-7-build-images.sh`. **Then check every dependency resolves inside each Node image**, because npm can "succeed" with nothing installed. Use `--no-cache` for any image with a bad layer |
| Recreate containers on existing volumes | the stack-up script from `orchestration/benchcore.py` (`stack_up_script()`), or `compose up -d --no-build` |
| Any WSL call from Windows | `wsl -d Ubuntu-22.04 -u root --exec bash -lc "…"` (use **`--exec`**, not `--`) |

## 5. Progress: next steps, in order

Status of the 2026-09-25 list:

- [x] **Commit M27:** done as `63d534b` + `ba48923`, now on `main` and `origin/main`.
- [x] **Re-smoke E0 after the refactor:** done on `f441636`, all 4 variants (§2.3).
- [ ] **Decide `events_per_case_per_round`: 200 or 224.** Still open. At 200, the multi-channel runs in E1-PA, E2
      and E3b fall sub-floor; the app's Preview shows it.
- [ ] **E0 ramp** (coarse send-rate ramp to find saturation), then "Use as baseline" and a finer E3a grid.
- [ ] **E1 → E2 through the app,** then send the variable table and the three flowcharts to D **before E3**.
- [ ] **E3a / E3b / per-operation breakdown** at the calibrated baselines; export the CSVs and charts.

New from this session:

- [x] **Merge and push the refactor:** merged into `main` as `6073abc` and pushed to `origin/main`.
- [x] **Replace the stale proxy certificate bundle:** `C:\Users\LENOVO\gleipnir-ca-bundle.crt` was regenerated
      on 2026-09-26.
  - It now holds 110 certificates from the Windows Root and intermediate (CA) stores, as UTF-8 without BOM.
  - Checked: a container reaches the npm registry through the proxy with it (HTTP 200).
  - To regenerate after the proxy changes again: PowerShell over `Cert:\LocalMachine\Root,
    Cert:\CurrentUser\Root, Cert:\LocalMachine\CA, Cert:\CurrentUser\CA`, de-duplicated by thumbprint.
- [x] **Clean-up:** the 10 `:pre-ponytail` image tags, the 4 older backups (only `backups/20260926-202333`
      remains) and the local `ponytail-refactor` branch are deleted.
- [x] **Align `docs/AS-BUILT.md`** with the refactor: a status note, plus the ~12 lines that named removed files,
      flags and routes.

## 6. Open decisions and stale notes

- **Still open with D:**
  - the baseline rule thresholds (§4.1–4.3);
  - the minimum-over-variants send-rate baseline;
  - the ½-saturation margin (`RAMP_MARGIN` 0.5);
  - whether E2's healthy rule needs a memory criterion.
- **Still open with the lecturer:** whether to keep the **off-chain B/event** metric.
- **Updated from 2026-09-25: "the SPA can't show the running variant."**
  - The **Merkle verify controls now work** without it: they follow each write's `batched` flag.
  - The designed **variant chip** is still skipped. It still needs an nginx route to the gateway's `/healthz`,
    which is a CONTRACTS change.
- **Existing small UI quirk (unchanged by the refactor):** after a manual "Log access", the on-chain custody
  list doesn't refresh until the page reloads. The session-events panel does update.
- **Not exercised live (residual risk):**
  - `registerEnroll.sh` (full, and the anchor-only path) and `up.sh` without `--skip-crypto`. Both only run on
    fresh crypto, which needs a wipe. The refactor's `fcc`/`enroll_node` changes there were checked by syntax
    and by reading only.
  - The Bench app's own Run → backup → restore dialogs. This session ran the same `benchcore` scripts directly.
- **The designers still owe us:** the brand mark (1a is the placeholder), exports and favicon, the lead and admin
  screens, the other Bench tabs, the court report, and the sample thesis figures.
- **Local `CLAUDE.md` is stale** (gitignored, so fix it by hand). It still describes the operator dashboard in its
  Frontend section and `workload.cases` in Experiment constants.

## 7. Rules that bit us (keep them)

- **Never** run `down.sh --wipe`, `docker compose down -v` or `docker volume rm` without explicit permission for
  that one time. Plain `down.sh` keeps the volumes.
  - The supported way around ledger resets is: back up → reset → restore.
  - Check the data with a fingerprint through the routes that don't add ACCESS events.
- **Four gateway routes add an ACCESS event when read:** `GET /evidence/:id`, `/download`, `/export` and
  `/cases/:id/coc-report`. Never open a fixture exhibit or its report while testing. Use throwaway smoke exhibits
  inside a backup/restore window.
- **Never type passwords into the browser.** Get a token with `POST /api/v1/auth/login` from a shell, then
  `sessionStorage.setItem('gleipnir.session', token)` and reload.
- **Check the dependencies inside a rebuilt image.** npm can exit 0 with nothing installed when TLS through the
  proxy fails.
- **Rebuild images together with compose edits,** and **don't patch files with inline Bash heredocs** that
  contain backslashes.

## 8. Files, for orientation

- **Added:** `frontend/src/components/TeamRoster.tsx`, `frontend/src/lib/save.ts`, `docs/handoff-2026-09-26.md`.
- **Removed:**
  - `benchmark/networks/parallel-c{1,5,10,20,30,40,50}.yaml`, `benchmark/networks/case-template.yaml`,
    `benchmark/benchmarks/smoke-*.yaml`
  - `orchestration/teardown-channel.sh`
  - `gateway/src/{batcherClient,receiptsClient}.js`
  - `frontend/src/settings.tsx`, `frontend/src/pages/shared/{NotFound,Unauthorized}Page.tsx`
- **Contract record:** `docs/CONTRACTS.md` §12-20 (plus dated "removed 2026-09-26" notes on §12-8, 12-9, 12-13,
  12-15 and 12-18).
- **Local only (gitignored):** `benchmark/results/e0/` (new E0 runs), `benchmark/results/e0-pre-ponytail-20260926/`
  (archive), `backups/20260926-202333/` (backup A).
