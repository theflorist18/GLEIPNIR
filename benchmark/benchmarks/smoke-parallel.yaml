# GENERATED by orchestration/rounds.py from benchmark/sweeps.yaml — do not edit by hand.
# SMOKE test — functional correctness only (regimes.smoke: 10 cases x 10-25 logs at 5 tx/s).
# Its results MUST NOT be cited for any scalability/throughput claim (CLAUDE.md, ARCHITECTURE §8).
# experiment.py --exp e0 renders the same rounds one launch at a time; this file is for manual launches.
# network config: networks/parallel-c1.yaml
test:
  name: gleipnir-smoke-parallel
  description: Smoke functional check of the parallel variant (labels smoke-*; regime smoke).
  workers:
    number: 4
  rounds:
  - label: smoke-create
    txNumber: 20
    rateControl:
      type: fixed-rate
      opts:
        tps: 5
    workload:
      module: workload/createEvidence.js
      arguments:
        mode: fabric
        label: smoke-create
        channels: 1
        variant: parallel
        payloadBytes: 256
  - label: smoke-logs
    txNumber: 250
    rateControl:
      type: fixed-rate
      opts:
        tps: 5
    workload:
      module: workload/accessLog.js
      arguments:
        mode: fabric
        label: smoke-logs
        channels: 1
        variant: parallel
        pool: 20
        payloadBytes: 256
monitors:
  resource:
  - module: docker
    options:
      interval: 5
      containers:
      - /orderer0.example.com
      - /orderer1.example.com
      - /orderer2.example.com
      - /peer0.org1.example.com
      - /peer0.org2.example.com
      - /ccaas-evidence
      - /gleipnir-gateway
