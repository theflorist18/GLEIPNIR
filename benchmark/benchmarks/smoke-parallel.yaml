# GENERATED by generate-rounds.js from sweeps.yaml — do not edit by hand.
# SMOKE test — functional correctness only (regimes.smoke: 10 cases x 10-25 logs). Its results MUST NOT be
# cited for any scalability/throughput claim (CLAUDE.md, ARCHITECTURE §8).
# Single-channel smoke (case-001); use networks/parallel-c1.yaml.
test:
  name: gleipnir-smoke-parallel
  description: Smoke functional check of the Parallel variant (single case channel).
  workers:
    number: 2
  rounds:
    - label: smoke-create
      txNumber: 10
      rateControl:
        type: fixed-rate
        opts:
          tps: 5
      workload:
        module: workload/createEvidence.js
        arguments:
          mode: fabric
          channels: 1
    - label: smoke-logs
      txNumber: 250
      rateControl:
        type: fixed-rate
        opts:
          tps: 25
      workload:
        module: workload/accessLog.js
        arguments:
          mode: fabric
          scenario: spread
          pool: 10
          channels: 1
