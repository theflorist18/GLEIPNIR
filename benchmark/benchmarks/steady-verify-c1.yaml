# GENERATED by generate-rounds.js from sweeps.yaml — do not edit by hand.
# STEADY-STATE — VERIFICATION latency (thesis RQ2): fetch receipt -> recompute
# branch -> compare anchored root, driven through the gateway (rest connector;
# use networks/rest-gateway.yaml). Seeding + /flush happen in the untimed init.
test:
  name: gleipnir-steady-verify-c1
  description: Verification latency, Parallel-Anchored 1-channel cell (seeds spread per case).
  workers:
    number: 4
  rounds:
    - label: verify-50tps-c1
      txNumber: 1000
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workload/verify.js
        arguments:
          seedCount: 100
          channels: 1
    - label: verify-fixed-load-c1
      txDuration: 60
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 5
          startTps: 50
      workload:
        module: workload/verify.js
        arguments:
          seedCount: 100
          channels: 1
