# GENERATED by generate-rounds.js from sweeps.yaml — do not edit by hand.
# SMOKE test — functional correctness only (regimes.smoke: 10 cases x 10-25 logs). Its results MUST NOT be
# cited for any scalability/throughput claim (CLAUDE.md, ARCHITECTURE §8).
test:
  name: gleipnir-smoke-anchoring
  description: Smoke functional check of the Anchoring variant (enqueue + verify round-trip).
  workers:
    number: 2
  rounds:
    - label: smoke-create
      txNumber: 10
      rateControl:
        type: fixed-rate
        opts:
          tps: 5
      workload:
        module: workload/createEvidence.js
        arguments:
          mode: rest
    - label: smoke-logs
      txNumber: 250
      rateControl:
        type: fixed-rate
        opts:
          tps: 25
      workload:
        module: workload/accessLog.js
        arguments:
          mode: rest
          scenario: spread
          pool: 10
    - label: smoke-verify
      txNumber: 100
      rateControl:
        type: fixed-rate
        opts:
          tps: 10
      workload:
        module: workload/verify.js
        arguments:
          seedCount: 10
