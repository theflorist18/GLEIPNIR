# GENERATED by generate-rounds.js from sweeps.yaml — do not edit by hand.
# STEADY-STATE — Anchoring variant (rest-mode; use networks/rest-gateway.yaml).
test:
  name: gleipnir-steady-anchoring
  description: Anchoring variant steady-state — create/access via the batcher enqueue path.
  workers:
    number: 4
  rounds:
    - label: create-25tps
      txNumber: 1000
      rateControl:
        type: fixed-rate
        opts:
          tps: 25
      workload:
        module: workload/createEvidence.js
        arguments:
          mode: rest
    - label: create-50tps
      txNumber: 1000
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workload/createEvidence.js
        arguments:
          mode: rest
    - label: create-100tps
      txNumber: 1000
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/createEvidence.js
        arguments:
          mode: rest
    - label: access-spread-50tps
      txNumber: 1000
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workload/accessLog.js
        arguments:
          mode: rest
          scenario: spread
          pool: 25
