# GENERATED by orchestration/rounds.py from benchmark/sweeps.yaml — do not edit by hand.
# SMOKE test — functional correctness only (regimes.smoke: 10 cases x 10-25 logs at 5 tx/s).
# Its results MUST NOT be cited for any scalability/throughput claim (CLAUDE.md, ARCHITECTURE §8).
# experiment.py --exp e0 renders the same rounds one launch at a time; this file is for manual launches.
# network config: networks/rest-gateway.yaml
test:
  name: gleipnir-smoke-parallel-anchored
  description: Smoke functional check of the parallel-anchored variant (labels smoke-*; regime smoke).
  workers:
    number: 4
  rounds:
  - label: smoke-create
    txNumber: 20
    rateControl:
      type: fixed-rate
      opts:
        tps: 5
    workload:
      module: workload/createEvidence.js
      arguments:
        mode: rest
        label: smoke-create
        channels: 1
        variant: parallel-anchored
        payloadBytes: 256
  - label: smoke-logs
    txNumber: 250
    rateControl:
      type: fixed-rate
      opts:
        tps: 5
    workload:
      module: workload/accessLog.js
      arguments:
        mode: rest
        label: smoke-logs
        channels: 1
        variant: parallel-anchored
        pool: 20
        payloadBytes: 256
  - label: smoke-verify
    txNumber: 100
    rateControl:
      type: fixed-rate
      opts:
        tps: 5
    workload:
      module: workload/verify.js
      arguments:
        mode: rest
        label: smoke-verify
        channels: 1
        variant: parallel-anchored
        seedCount: 10
monitors:
  resource:
  - module: docker
    options:
      interval: 5
      containers:
      - /orderer0.example.com
      - /orderer1.example.com
      - /orderer2.example.com
      - /peer0.org1.example.com
      - /peer0.org2.example.com
      - /ccaas-evidence
      - /gleipnir-gateway
      - /gleipnir-merkle-batcher
      - /gleipnir-receipt-store
      - /gleipnir-verification
      - /peer0.anchor.example.com
      - /ccaas-evidence-anchor
      - /gleipnir-anchor-client
