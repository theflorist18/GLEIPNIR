{
  "name": "gleipnir-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "GLEIPNIR API gateway / backend-for-frontend: the only holder of fabric-gateway sessions to the app channels; encapsulates variant routing.",
  "license": "MIT",
  "type": "commonjs",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.0",
    "@hyperledger/fabric-gateway": "1.11.0",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  }
}
